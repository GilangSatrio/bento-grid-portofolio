# Profile photo

Add Gilang's real professional portrait as `gilang-satrio.jpg` in this folder. The profile card will use it automatically and retains the GS initials as a fallback until the image is added.
